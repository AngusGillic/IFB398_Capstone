import 'package:flutter/cupertino.dart';

/// Kept as plain const colours so existing screens can still use them inside
/// const constructors. Values are the iOS system palette.
class AppColors {
  static const screen = Color(0xFFF2F2F7);
  static const panel = Color(0xFFFFFFFF);
  static const panelDark = Color(0xFFE5E5EA);
  static const green = Color(0xFF34C759);
  static const greenDark = Color(0xFF248A3D);
  static const mintPanel = Color(0xFFE4F6EC);
  static const text = Color(0xFF000000);
  static const greyText = Color(0xFF8E8E93);
  static const iconGrey = Color(0xFFAEAEB2);
  static const navGrey = Color(0xFFF2F2F7);

  static const accent = Color(0xFF007AFF);
  static const separator = Color(0x4D3C3C43);
}
